{% extends 'base.html.twig' %}

{% block title 'EcoRide Editer : ' ~ ride.departure %}

{% block body %}
    {{ride.slug}}

    <turbo-frame id="ride_{{ ride.id }}">
        {{ form(rideForm, {action: path('ride.mine.edit', {id: ride.id})}) }}
    </turbo-frame>

{% endblock %}

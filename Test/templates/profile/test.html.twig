{% extends 'base.html.twig' %}

{% block title 'EcoRide profile : ' %}

{% block body %}

    {{ user.username }}

    <h3> Commentaires</h3>
    {% if app.user %}
        {{form(commentForm)}}
    {% endif %}

    {% for comment in comments %}
        {% include 'comment/comment.html.twig'%}
    {% endfor %}

{% endblock %}
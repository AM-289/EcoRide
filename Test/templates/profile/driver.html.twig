{% extends 'base.html.twig' %}

{% block title 'EcoRide profile : ' ~ user.username %}

{% block body %}

<a class="btn btn-primary" href={{path('profile.slug.edit', {slug: user.slug})}}>Editer</a>
{% endblock %}